/**
 * Regenerar con: npm run supabase:types (requiere CLI y SUPABASE_PROJECT_ID).
 * Mientras tanto, tipado laxo para no bloquear el build.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
